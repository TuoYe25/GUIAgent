{
  "name": "edge-gui-agent-electron",
  "version": "0.1.0",
  "description": "Edge Device GUI Agent Electron Demo",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "electron-builder",
    "pack": "electron-builder --dir"
  },
  "dependencies": {
    "electron": "^30.0.0",
    "playwright": "^1.42.0"
  },
  "devDependencies": {
    "electron-builder": "^24.13.0"
  },
  "build": {
    "appId": "com.edge.gui-agent-demo",
    "productName": "Edge GUI Agent",
    "directories": {
      "output": "dist"
    }
  }
}
